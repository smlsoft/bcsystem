apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: vfmerchant-uat-dedepos-com-cluster-issuer
  namespace: cert-manager
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: admin@example.com
    privateKeySecretRef:
      name: vfmerchant-uat-dedepos-com-tls-secert
    solvers:
      - http01:
          ingress:
            class: nginx
